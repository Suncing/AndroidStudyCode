<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a1_basics">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListViewArrayAdapter"></activity>
        <activity android:name=".ListViewDifficultActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ListViewSimpleActivity" />
        <activity android:name=".ActionBarViewActivity" />
        <activity android:name=".ActionBarActivity" />
        <activity android:name=".ActionBarItemActivity" />
        <activity android:name=".ClickActivity" />
        <activity android:name=".CallActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>